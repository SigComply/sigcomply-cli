# =============================================================================
# SigComply CLI - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control (it's in .gitignore).
#
# For full documentation: docs/configuration.md

# -----------------------------------------------------------------------------
# Provider Credentials (REQUIRED - at least one provider)
# -----------------------------------------------------------------------------

# AWS — Uses the standard AWS SDK credential chain.
# Option 1: Static credentials (local development)
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...              # Only if using temporary credentials
#
# Option 2: AWS CLI profile
# AWS_PROFILE=my-profile
#
# Option 3: IAM role / OIDC (CI/CD, ECS, EKS) — no env vars needed.

# GitHub — Required only if checking GitHub resources.
# GITHUB_TOKEN=ghp_...               # Personal access token or GitHub App token

# -----------------------------------------------------------------------------
# SigComply Settings (OPTIONAL — override config file / defaults)
# -----------------------------------------------------------------------------

# Framework to evaluate (default: soc2)
# SIGCOMPLY_FRAMEWORK=soc2            # Options: soc2, iso27001, hipaa

# Output format (default: text)
# SIGCOMPLY_OUTPUT_FORMAT=text        # Options: text, json, sarif, junit

# Verbose output
# SIGCOMPLY_VERBOSE=true

# Exit code behavior — set to "false" to always exit 0
# SIGCOMPLY_FAIL_ON_VIOLATION=true

# -----------------------------------------------------------------------------
# Provider Settings (OPTIONAL — override config file)
# -----------------------------------------------------------------------------

# AWS region override (single region; for multiple regions use .sigcomply.yaml)
# SIGCOMPLY_AWS_REGION=us-east-1

# GitHub organization
# SIGCOMPLY_GITHUB_ORG=my-org

# -----------------------------------------------------------------------------
# Evidence Storage (OPTIONAL)
# -----------------------------------------------------------------------------

# Enable evidence storage
# SIGCOMPLY_STORAGE_ENABLED=true

# Storage backend (default: local)
# SIGCOMPLY_STORAGE_BACKEND=local     # Options: local, s3

# Local storage path (when backend=local)
# SIGCOMPLY_STORAGE_PATH=./.sigcomply/evidence

# S3 storage (when backend=s3)
# SIGCOMPLY_STORAGE_BUCKET=my-compliance-bucket
# SIGCOMPLY_STORAGE_REGION=us-east-1
# SIGCOMPLY_STORAGE_PREFIX=compliance/

# -----------------------------------------------------------------------------
# SigComply Cloud (OPTIONAL — paid tier)
# -----------------------------------------------------------------------------

# API token — auto-enables cloud submission when set.
# SIGCOMPLY_API_TOKEN=sc_...
