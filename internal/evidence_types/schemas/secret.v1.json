{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.sigcomply.io/evidence_types/secret/v1.json",
  "title": "secret",
  "version": 1,
  "description": "One secret stored in a managed secrets service — one record per AWS Secrets Manager secret, GCP Secret Manager secret, Azure Key Vault secret. Policies check rotation and encryption.",
  "type": "object",
  "required": ["id", "name", "rotation_enabled", "last_rotated_days", "kms_encrypted"],
  "properties": {
    "id":               { "type": "string", "description": "Unique secret identifier (ARN, resource name)." },
    "name":             { "type": "string", "description": "Human-readable secret name." },
    "provider":         { "type": "string", "description": "Cloud provider short name: aws, gcp, azure." },
    "rotation_enabled": { "type": "boolean", "description": "Automatic rotation is configured." },
    "last_rotated_days":{ "type": "integer", "description": "Days since the secret was last rotated. -1 if never rotated or data unavailable." },
    "kms_encrypted":    { "type": "boolean", "description": "Secret is encrypted with a customer-managed KMS key." }
  },
  "additionalProperties": true
}
