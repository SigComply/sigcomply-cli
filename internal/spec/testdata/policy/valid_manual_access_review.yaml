schema_version: policy.v1

id: soc2.cc1.4.quarterly_access_review
control: SOC2.CC1.4
severity: medium
category: governance

cadence: quarterly
# on_push omitted — defaults to false for evidence_mode: manual

evidence_mode: manual
catalog_entry: access_review_quarterly

description: |
  An access review of all privileged users must be completed and
  signed off each quarter.

parameters:
  grace_period_days:
    type: int
    default: 30
    min: 0
    max: 90
