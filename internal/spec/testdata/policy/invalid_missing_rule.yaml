schema_version: policy.v1
id: soc2.cc6.1.mfa_enforced
control: SOC2.CC6.1
severity: high
cadence: daily
evidence_mode: automated
description: "All users must have MFA enabled."
slots:
  user_directory:
    accepts: [directory_user]
    cardinality: one-or-more
    required: true
# deliberately omitting both pass_when and rule — should fail validation
