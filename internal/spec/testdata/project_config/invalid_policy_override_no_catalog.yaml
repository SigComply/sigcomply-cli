schema_version: project.v1
framework: soc2
vault:
  backend: local
  path: ./vault

policy_overrides:
  soc2.cc6.1.mfa_enforced:
    evidence_mode: manual
    # catalog_entry missing — required when evidence_mode is manual
