schema_version: project.v1
framework: soc2
vault:
  backend: local
  path: ./vault
policies:
  soc2.cc6.1.mfa_enforced:
    evidence_mode: ""   # empty == no override; catalog_entry then not allowed
    catalog_entry: mfa_attestation
