schema_version: project.v1
framework: soc2
vault:
  backend: local
  path: ./vault

policy_overrides:
  soc2.cc6.1.mfa_enforced:
    evidence_mode: automated
    catalog_entry: mfa_attestation  # must not be set for automated
