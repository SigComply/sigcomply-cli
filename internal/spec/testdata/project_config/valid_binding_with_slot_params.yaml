schema_version: project.v1

framework: soc2

vault:
  backend: local
  path: ./vault

sources:
  aws.iam:
    region: us-east-1

bindings:
  soc2.cc6.1.admin_mfa_enforced:
    user_directory:
      - source: aws.iam
        slot_params:
          filter_admins_only: true
