---
name: Bug report
about: Report a defect in the SigComply CLI
title: "bug: <short summary>"
labels: bug
assignees: ''
---

<!--
Thanks for helping improve SigComply. Please DO NOT paste raw evidence,
credentials, ARNs, emails, or other identifiers. SigComply is non-custodial —
keep it that way in issues too. Redact anything sensitive from logs before
posting.
-->

## What happened

<!-- A clear description of the bug. -->

## What you expected

<!-- What you expected to happen instead. -->

## Steps to reproduce

1.
2.
3.

## Command and output

<!-- The exact `sigcomply ...` command, and its output + exit code.
     Re-run with `--verbose` for more detail. Redact identifiers first. -->

```
$ sigcomply <command>
<redacted output>
$ echo $?
<exit code>
```

## Environment

- CLI version (`sigcomply version`):
- OS / arch (e.g. macOS arm64, Ubuntu 24.04 x86_64):
- CI platform, if any (GitHub Actions / GitLab CI / local):
- Framework (`soc2` / `iso27001`):
- Relevant `.sigcomply.yaml` (redacted — no credentials):

```yaml
```

## Additional context

<!-- Anything else that might help — when it started, workarounds tried, etc. -->
